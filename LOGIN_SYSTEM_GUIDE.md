# 🔐 網站登入系統使用指南

## 系統概述

你的網站現在已整合完整的登入和權限管理系統。訪客需要用正確的密碼才能訪問受保護的頁面。

---

## 🚀 快速開始

### 訪客如何訪問受保護內容？

1. 訪客點擊任何受保護的頁面（如「關於藝術家」、「表演紀錄」等）
2. 如果未登入，會被自動重定向到 **登入頁面** (`login.html`)
3. 輸入正確的密碼：`hlc*0930`
4. 登入成功後，可以訪問所有受保護的頁面

### 你如何管理權限？

1. 作為藝術家，你已永久登入
2. 在導航菜單中點擊 **⚙️ 管理後台** 按鈕
3. 進入管理後台後，可以：
   - 在「頁面權限」中切換各頁面的開放/鎖定狀態
   - 在「基本設置」中調整系統設定
   - 隨時「登出」並清除登入狀態

---

## 📋 各頁面說明

### 主要頁面

| 頁面 | 文件名 | 預設狀態 | 說明 |
|------|--------|--------|------|
| 首頁 | `index.html` | 🔓 開放 | 總是可見，無需登入 |
| 關於藝術家 | `about.html` | 🔒 鎖定 | 需要登入才能查看 |
| 表演紀錄 | `performance-records.html` | 🔒 鎖定 | 需要登入才能查看 |
| 作品集 | `portfolio.html` | 🔒 鎖定 | 需要登入才能查看（可添加） |
| 其他項目 | `projects.html` | 🔒 鎖定 | 需要登入才能查看（可添加） |

### 特殊頁面

| 頁面 | 文件名 | 用途 |
|------|--------|------|
| 登入頁 | `login.html` | 訪客輸入密碼的地方 |
| 管理後台 | `admin.html` | 你控制權限的地方 |

---

## 🔧 系統架構

### 主要文件

1. **login.html** - 登入頁面
   - 簡潔的登入界面
   - 驗證密碼（hardcoded 為 `hlc*1373`）
   - 成功後在 localStorage 中設置 `hlc_authenticated` 為 `'true'`

2. **admin.html** - 管理後台
   - 頁面權限管理
   - 基本系統設置
   - 只有登入用戶才能訪問

3. **js/access-control.js** - 訪問控制核心
   - 檢查用戶是否登入
   - 驗證頁面權限
   - 攔截未授權訪問並重定向到登入頁

4. **修改的頁面** - 已集成訪問控制
   - `index.html` - 已添加訪問控制和登入按鈕
   - `about.html` - 已添加訪問控制和登入按鈕
   - `performance-records.html` - 已添加訪問控制和登入按鈕

---

## 🎯 工作流程

### 訪客的流程

```
訪問受保護頁面
    ↓
[檢查登入狀態]
    ↓
未登入？ → 重定向到登入頁面 → 輸入密碼 → 驗證 → 成功：設置登入狀態
    ↓                                        ↓
已登入 → 正常顯示頁面                      失敗：提示錯誤，清除輸入
```

### 藝術家的流程

```
訪問任何頁面
    ↓
[自動偵測為管理員]
    ↓
顯示導航菜單中的 "⚙️ 管理後台" 按鈕
    ↓
進入管理後台
    ↓
[修改頁面權限]
    ↓
設置保存到 localStorage (hlc_page_permissions)
    ↓
訪客下次訪問時會看到更新的權限設置
```

---

## 💾 數據存儲

系統使用瀏覽器 **localStorage** 存儲數據（不需要後端）：

### localStorage 鍵值

| 鍵 | 值 | 說明 |
|----|-----|------|
| `hlc_authenticated` | `'true'` 或 `undefined` | 登入狀態 |
| `hlc_page_permissions` | JSON 物件 | 各頁面的權限設置 |
| `hlc_setting_*` | `'true'` 或 `'false'` | 各項系統設置 |

### 預設權限結構

```javascript
{
  "about.html": true,                    // true = 鎖定，false = 開放
  "performance-records.html": true,
  "portfolio.html": true,
  "projects.html": true,
  "index.html": false                    // 首頁總是開放
}
```

---

## 🔑 密碼相關

### 當前密碼

```
hlc*1373
```

### 如何更改密碼？

編輯 `login.html` 中的這一行：

```javascript
const CORRECT_PASSWORD = 'hlc*1373';  // ← 改成你想要的密碼
```

然後保存文件。訪客下次登入時會使用新密碼。

---

## ⚠️ 重要注意事項

### 1. **localStorage 是局部的**
   - 每個瀏覽器/設備的登入狀態互不影響
   - 清除瀏覽器 Cookies/數據會登出所有用戶
   - 訪客可以通過打開「隱私模式」來避免長期存儲登入狀態

### 2. **密碼以明文存儲**
   - 密碼直接寫在 JavaScript 代碼中
   - 這對本地網站或內網展示很合適
   - **不建議用於涉及真實敏感資訊的網站**

### 3. **沒有後端驗證**
   - 整個系統基於客端 JavaScript
   - 訪客可以通過瀏覽器開發者工具修改權限
   - 適合讓朋友/家人看作品的場景，不適合高安全性需求

### 4. **首頁總是開放**
   - `index.html` 設置為始終開放（不可更改）
   - 確保訪客總能看到你的作品介紹

---

## 🐛 常見問題

### Q: 訪客登入後多久會自動登出？
A: 登入狀態永久保存在浏览器 localStorage 中，直到訪客手動清除或登出。

### Q: 我忘記了密碼怎麼辦？
A: 編輯 `login.html` 文件中的 `CORRECT_PASSWORD` 變量，設置新密碼。

### Q: 如何重置所有權限回到預設狀態？
A: 在瀏覽器開發者工具的 Console 中執行：
```javascript
localStorage.removeItem('hlc_page_permissions');
```
然後刷新管理後台頁面。

### Q: 多個訪客可以同時登入嗎？
A: 可以，只要他們用的是不同的瀏覽器或設備。

### Q: 我可以添加新的受保護頁面嗎？
A: 可以，在你的新頁面中：
1. 在 `<head>` 添加：`<script src="js/access-control.js"></script>`
2. 在導航中添加登入按鈕容器：`<div id="login-button-container"></div>`
3. 在 `access-control.js` 的 `DEFAULT_PERMISSIONS` 中添加該頁面

---

## 📞 技術支持

如需修改或擴展功能，主要涉及以下文件：

- **修改登入邏輯** → 編輯 `js/access-control.js`
- **修改登入界面** → 編輯 `login.html`
- **修改管理後台界面** → 編輯 `admin.html`
- **更改密碼** → 編輯 `login.html` 中的密碼常量

---

## ✨ 功能已完成

✅ 登入系統  
✅ 頁面權限控制  
✅ 管理後台  
✅ 訪問攔截和重定向  
✅ 所有頁面集成

---

祝你使用愉快！🎉
